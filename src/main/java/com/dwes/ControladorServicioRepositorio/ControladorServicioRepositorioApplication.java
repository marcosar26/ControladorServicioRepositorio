package com.dwes.ControladorServicioRepositorio;

import org.springframework.boot.SpringApplication;
import org.springframework.boot.autoconfigure.SpringBootApplication;

@SpringBootApplication
public class ControladorServicioRepositorioApplication {

	public static void main(String[] args) {
		SpringApplication.run(ControladorServicioRepositorioApplication.class, args);
	}

}
